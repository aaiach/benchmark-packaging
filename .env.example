# =============================================================================
# Environment Variables - Copy this file to .env and fill in the values
# =============================================================================
# This file works for both development and production environments.
# The same .env file is used by docker-compose.yml (dev) and docker-compose.prod.yml
# =============================================================================

# -----------------------------------------------------------------------------
# API Keys (Required for scraper pipeline)
# -----------------------------------------------------------------------------
OPENAI_API_KEY=your_openai_api_key_here
GOOGLE_API_KEY=your_google_api_key_here
FIRECRAWL_API_KEY=your_firecrawl_api_key_here

# -----------------------------------------------------------------------------
# Optional API Keys (for extended functionality)
# -----------------------------------------------------------------------------
ANTHROPIC_API_KEY=
DEEPGRAM_API_KEY=
ASSEMBLYAI_API_KEY=

# -----------------------------------------------------------------------------
# Celery Configuration (Redis)
# -----------------------------------------------------------------------------
# These are set in docker-compose files, but can be overridden here
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/1

# -----------------------------------------------------------------------------
# Output Directory
# -----------------------------------------------------------------------------
# Path where scraper output (images, analysis) is stored
# In Docker: /app/data/output (mounted volume)
OUTPUT_DIR=/app/data/output

# -----------------------------------------------------------------------------
# Flask Configuration
# -----------------------------------------------------------------------------
# Development: development, Production: production
FLASK_ENV=development
FLASK_DEBUG=1
FLASK_APP=src.app:create_app

# -----------------------------------------------------------------------------
# CORS Configuration (Production only)
# -----------------------------------------------------------------------------
# Comma-separated list of allowed origins for CORS
# In development, leave empty or set to * (allow all)
# In production, set to your domain(s)
# Example: https://packaging-benchmark.tryiceberg.ai
CORS_ORIGINS=*

# -----------------------------------------------------------------------------
# Flower Monitoring (Production optional)
# -----------------------------------------------------------------------------
# Basic auth for Flower dashboard (format: username:password)
# Leave empty to disable authentication (not recommended for production)
# Example: admin:secure_password_here
FLOWER_BASIC_AUTH=

# -----------------------------------------------------------------------------
# Frontend Configuration (for reference - set via Docker build args)
# -----------------------------------------------------------------------------
# These are baked into the frontend at build time, not runtime
# Development: http://localhost:5000
# Production: https://packaging-benchmark.tryiceberg.ai
# VITE_API_URL=http://localhost:5000
